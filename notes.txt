
To-do
* Date selection need to be redesigned, as one scrolls it is trying to also select the dates, and it's difficult to go far back in time in terms of years
* Check angel for all the chars, the c unit may not be correct
* We need to note when the user does something really computationally demanding, like adding 100 data series
* Re-implement the rounding used in OpenCelerator
* Legend doesn't scale with chart size
* Sometimes it zooms in, in a way that breaks the chart dimensions and requires a refresh to undo
* Changing the start date on non-daily chart is currently buggy, likely because somewhere in the code, there's an assumption about us still using the daily chart
* Phase lines don't land where I click
* Make sure that minute column is excluded from count on export
* Maybe a fading or transition phase to spare the user from seeing the credit lines and chart adjust in size
* Having instructions on both the right and left side is confusing.
* When adding data points with data entry, it doesn't add the non occurrences under the floor and draws the line from whatever the previous point landed, so a line can go in the wrong direction
-- A refresh fixes the missing floor data point, but not the line going in the wrong direction
* Might be using a UUID hack right now
* What's up with the chartStorage.js reproducing the json?
* Is the cel line added even when there's less than 5 data points?
* I need to check that my PWA is not relying on CDN that will break the app in offline mode
* We might need a button for line editing instead of long press...
* Cel line won't appear in misc all_monthly data
-- reintroduces segments of the fan
-- Doesn't show names for the misc series when selected trend type.
* Check whether the underlying daily celeraiton is calculated in each case to get the right one
* Toggle for placing below floor can be slow to respond
* Simplify the trend line coloring system
* When exporting from a non-minute chart, discard minutes
* Reset panning when changing chart capacity
* First data point is set on the right date
* Legend still uses phase and aim names
* timing inputs in data entry is not hidden immediately
* The click to add date lines is driving me nuts
-- Maybe make it work temporarily on a timer after having 1) just navigated to the data entry menu or 2) used the arrows or dates?
* Show the interactivity recentangle (should be slightly longer) BEFORE clicking, but not when dragging.
* Phase line feedback shouldn't have double notifications when adding phase lines
* Chart dimensions changes across browswers, at least with fonts, but maybe mo
* Phase lines don't sync across charts
-- Might be because they don't trigger a save event
---- Except that they do?
* Line type is missing from the style settings, and lines and marker settings should have their own grouping
* Crosshair doesn't actually have a centered clickpoint
- There's a distortion when auto resizing
- Check out the usage of config.maxWindow, we might have to get rid of that one. It should use chartState.chartCapacity, I think
- changing the chart window doesn't sync automatically, but works on manual refresh
- "online/offline" status doesn't show up well on smaller windows
- When placing a change line, have the dropdown be vertical (maybe dedicated modal?) and don't show options that are hidden in the legend (check chartState?)
- edit mode doesn't work reliably: it shuts off when a line cut is removed, and doesn't seem want to start again after it's done
-- Also, when removing, is it also removing the associated text?
- There's no line handling for stacking data points at the moment
- There's no Ux for unsharing at the moment, although I think the infrastructure should be there...
- If the user deletes a shared chart, we need to make sure no errors or resurrections occur
- Panning STILL doesn't reset after a start-date/capcity change
- importing a chart json should generate a new UUID, and chart key?
- OpenCelerator json import doesn't seem to understand last updated: "Updated Jan 21, 1970"
- and, yet again, auto save is triggered without me doing anything when loading the chart
- The idea of saving 1s in count chart is a waste of space. Just auto generate it.
- xValues look a bit short. Are they full timestamps?
- Is there protection against a user find out someoeone else's UUID for a chart and attempting to overwrite it on the server?
- Default chart capacity doesn't seem to respect, or at least not for monthly (and the chart generated was not aligned with what was showing for chart capacity)
- What is the relationship between loadChart and initializeChart and their respective roles? Do a survey and drop report as .md in Docs.
- The "Navigation" button needs renaming.
- - just cut the auto-reziing on page change for now, require manual refresh for the time being,
- Fix the auto aggregation notificaiton issue
- - Is it deferring the save event until at the END of the chart drawing and stuff? It should set a bool and check it at the end.
- We need to get rid of the double notification when placing lines
- The celeration fan's x value position (tested on minute chart) doesn't move in good proportion to the width of the chart.
- We need the welcome screen: 1) Check local storage if an SCC user exists. If not, direct to a welcome screen where we guide the user through some simple options and set values in localStorage
-- Keep the Ux very clean and the same style as elswhere, white and same kind of blue-is. Greet the user with title: Standard Change Chart, and then ask below in smaller text above three buttons:
-- What will you be using the app for? Three options: Teaching, Business, and Both. Once the user has selected, we'll do two things, 1) add that in localStorage, and 2) generate the passphrase that we use as their identity (the infrastructure of that already exists)
-  I need a robust mechanism to update the users apps
- if I use cut series line, it resets the visiblity options for other things like the data series it seems
- grid settings changs does not trigger a save event, check for the other visiblity settings as well (in the case of the grid, the legend seems to remember that the grid is off, state of the grid is not being applied)
-- In the case of the visiblity of a series, not even the legend seems to remember