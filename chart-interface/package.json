{
  "name": "scc-chart-plugin",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node build/esbuild.config.js",
    "build:min": "node build/esbuild.config.js --minify"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
